{
  "name": "code-tutor-narrator",
  "displayName": "Code Tutor Narrator",
  "description": "Lets an agent drive VS Code as a tutor: scroll to code, highlight it, and narrate it aloud. Exposes an MCP server.",
  "version": "0.5.1",
  "publisher": "local",
  "engines": {
    "vscode": "^1.80.0"
  },
  "main": "./out/extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "scripts": {
    "compile": "tsc -p .",
    "watch": "tsc -p . --watch",
    "test": "tsc -p . && node test/test-mcp.js && node test/test-tts.js",
    "package": "vsce package --allow-missing-repository --skip-license",
    "install-ext": "code --install-extension $(ls -t *.vsix | head -1) --force"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/vscode": "^1.80.0",
    "typescript": "^5.7.2",
    "@vscode/vsce": "^3.2.1"
  },
  "contributes": {
    "commands": [
      {
        "command": "codeTutor.clear",
        "title": "Code Tutor: Clear highlight"
      },
      {
        "command": "codeTutor.showStatus",
        "title": "Code Tutor: Show server status"
      },
      {
        "command": "codeTutor.setApiKey",
        "title": "Code Tutor: Set ElevenLabs API Key"
      }
    ],
    "configuration": {
      "title": "Code Tutor Narrator",
      "properties": {
        "codeTutor.port": {
          "type": "number",
          "default": 51730,
          "description": "Loopback port for the MCP server."
        },
        "codeTutor.voice": {
          "type": "string",
          "default": "Samantha",
          "description": "macOS `say` voice used for narration. Applies ONLY to the `say` provider; ElevenLabs uses codeTutor.elevenlabs.voiceId. Run `say -v '?'` to list."
        },
        "codeTutor.rate": {
          "type": "number",
          "default": 175,
          "description": "Words per minute. Applies ONLY to the `say` provider — it is ignored entirely by `elevenlabs` and `command`. To speed up ElevenLabs narration use codeTutor.playbackRate."
        },
        "codeTutor.tts.provider": {
          "type": "string",
          "enum": [
            "say",
            "elevenlabs",
            "command"
          ],
          "default": "say",
          "enumDescriptions": [
            "macOS built-in speech. Free, offline, instant.",
            "ElevenLabs cloud TTS. Best quality, billed per character.",
            "Pipe the text to any command on stdin (Kokoro, Piper, a curl script)."
          ],
          "description": "Which speech backend to narrate with."
        },
        "codeTutor.tts.command": {
          "type": "string",
          "default": "",
          "description": "Shell command for the 'command' provider. Receives narration text on stdin and is responsible for playing it."
        },
        "codeTutor.elevenlabs.voiceId": {
          "type": "string",
          "default": "21m00Tcm4TlvDq8ikWAM",
          "description": "ElevenLabs voice id. Voice Library ids are paid-only via the API and fail with 402 on a free plan — use a default voice, or add a library voice to your workspace to get an id you own."
        },
        "codeTutor.elevenlabs.modelId": {
          "type": "string",
          "default": "eleven_flash_v2_5",
          "description": "eleven_flash_v2_5 is ~75ms and half the price; eleven_multilingual_v2 is richer but slower."
        },
        "codeTutor.elevenlabs.baseUrl": {
          "type": "string",
          "default": "https://api.elevenlabs.io",
          "description": "API base URL. Override for a proxy or a test stub."
        },
        "codeTutor.elevenlabs.stability": {
          "type": "number",
          "default": 0.5,
          "description": "0 is expressive and variable, 1 is flat and consistent."
        },
        "codeTutor.elevenlabs.similarityBoost": {
          "type": "number",
          "default": 0.75,
          "description": "How closely to match the reference voice."
        },
        "codeTutor.elevenlabs.speed": {
          "type": "number",
          "default": 1,
          "minimum": 0.7,
          "maximum": 1.2,
          "description": "Speech speed multiplier applied during synthesis. ElevenLabs rejects anything outside 0.7–1.2 — for faster narration than that, use codeTutor.playbackRate instead."
        },
        "codeTutor.playbackRate": {
          "type": "number",
          "default": 1,
          "minimum": 0.5,
          "maximum": 4,
          "description": "Speeds up playback locally, after synthesis. Stacks on top of elevenlabs.speed and has no 1.2 ceiling, so this is the knob for genuinely fast narration. Pitch is preserved. Costs nothing and does not invalidate cached audio."
        },
        "codeTutor.cacheAudio": {
          "type": "boolean",
          "default": true,
          "description": "Cache synthesised audio on disk, so repeating a walkthrough costs nothing."
        }
      }
    }
  }
}
